package controller;

/** Interface to represent ImageController which is the controller for editing images. */
public interface ImageController {

  /** Starts the image editor using the controller. */
  void start();
}
